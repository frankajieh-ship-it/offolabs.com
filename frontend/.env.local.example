# OFFO Launch Platform - Frontend Configuration

# API Configuration
# Set this to your backend API URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# Demo Mode
# Set to 'true' to use local storage instead of backend API
# Set to 'false' or remove to connect to real backend
NEXT_PUBLIC_DEMO_MODE=false

# Feature Flags (optional)
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# ============================================================================
# Supabase Configuration (for Pilot Application Backend)
# ============================================================================
# Get these values from your Supabase project settings (https://app.supabase.com)

# Public Supabase URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (SERVER-SIDE ONLY - DO NOT EXPOSE TO CLIENT)
# This key bypasses Row Level Security and should NEVER be used in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# IMPORTANT SECURITY NOTES:
# ============================================================================
# 1. NEVER commit .env.local to version control
# 2. NEVER expose SUPABASE_SERVICE_ROLE_KEY to the client
# 3. Only use SUPABASE_SERVICE_ROLE_KEY in API routes (server-side)
# 4. The NEXT_PUBLIC_* variables are safe to use in client components
# ============================================================================

# Optional: Production Rate Limiting (Upstash Redis)
# Uncomment and configure if you want to use Redis for rate limiting instead of in-memory
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Optional: Email Notifications (Resend)
# Uncomment and configure if you want to send email notifications for new applications
# RESEND_API_KEY=

# ============================================================================
# Admin Authentication (for Admin Dashboard)
# ============================================================================
# Admin username
ADMIN_USERNAME=admin

# Admin password hash (SHA-256 hash of your password)
# To generate a hash for your password:
# 1. Open Node.js REPL: node
# 2. Run: crypto.createHash('sha256').update('your-password-here').digest('hex')
# 3. Copy the output and paste it here
# IMPORTANT: Change the default password in production!
ADMIN_PASSWORD_HASH=your-password-hash-here
